using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using System.Linq;

namespace Scripts
{
    public class ApplyButton : MonoBehaviour
    {
        /*
        public GameObject rcvPanel;
        public GameObject counterbackPanel;
        public Dropdown tradeMenu;
        public TradeManager TM;
        public GameManager GM;


        public void apply()
        {
            TradeManager TM = GameObject.Find("TradeManager").GetComponent<TradeManager>();
            GameManager GM = GameObject.Find("GameManager").GetComponent<GameManager>();
            tradeMenu = GameObject.Find("TradeList").GetComponent<Dropdown>();
            HeroController herocl = GameObject.Find(GM.GetHeroByPID(HeroSelection.HS.mySelectedCharacter)).GetComponent<HeroController>();
            Hero hero = herocl.getHero();

            Trade t = TM.GetHerosActiveTrades(hero).ElementAt(tradeMenu.value);

            if (t.getHero2() == hero)
            {
                counterbackPanel.SetActive(true);
            }
            else if (t.getHero1() == hero)
            {
                rcvPanel.SetActive(true);
            }


        }
        */
    }
}
